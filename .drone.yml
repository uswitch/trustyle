pipeline:
  install:
    when:
      event: [push]
    image: node:12.14.1
    commands:
      - npm install
      - npx lerna exec npm install
      - npx lerna bootstrap

  code_checks:
    when:
      event: [push]
    image: node:12.14.1
    commands:
      - npm run eslint:check
      - npm run prettier:check

  build:
    when:
      event: [push]
    image: node:12.14.1
    commands:
      - npx lerna run build

  send-percy:
    when:
      event: [push]
    image: node:12.14.1
    commands:
      - npm run snapshot
